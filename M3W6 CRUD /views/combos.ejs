<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Combos</title>
  <style>
        body {
      padding: 30px;
    }

    h1 {
      font-size: 60px;
      text-align: center;
    }

    section, nav {
      padding: 40px 20px;
      max-width: 1200px;
      margin: 0 auto;
      text-align: center;
    }

    h2 {
      font-size: 32px;
    }

    h3 {
      font-size: 28px;
    }

    div {
      text-align: left;
      border: 3px solid tomato;
      padding: 20px;
    }
    
    nav {
      display: flex;
      justify-content: space-around;
    }

    .button {
      color: white;
      padding: 10px 20px;
      background-color: rgb(205, 39, 10);
      border-radius: 5px;
      text-decoration: none;
    }

    .button:hover {
      background-color: rgb(247, 130, 109);
      color: black;
    }
    .no-borders {
      border: none;
    }

    .select-container {
      display: flex;
      justify-content: space-around;
      align-items: center;
    }
  </style>
</head>
<body>
  <h1>Awesome Fast Food place! With Toast!</h1>
  <h1><%= combos[3].main%> </h1>

  <% for(const item in combos) { %>
    <%= combos[item].main %>
  <% } %>
  
  <!-- <section>
    <% for(const combo in combos) { %>
      <div>
        <h3>Combo # <%= combo %></h3>
        <h3>Main: <%= combos[combo].main %></h3>
        <h3>Side: <%= combos[combo].side %></h3>
        <h3>Drink: <%= combos[combo].drink %></h3>
        <form action="/combos/edit/<%= combo %>" method="POST">
          <div>
            <label for="Main">Main</label>
            <input type="text" name="main">
          </div>
          <div>
            <label for="Side">Side</label>
            <input type="text" name="side">
          </div>
          <div>
            <label for="Drink">Drink</label>
            <input type="text" name="drink">
          </div>
          <button type="submit">Edit</button>
        </form>
        <form action="/combos/delete/<%= combo %>" method="POST">
          <button>Delete</button>
        </form>
      </div>
    <% } %>
  </section> -->
</body>
</html>